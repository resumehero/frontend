import { DndFileDirective } from './dnd-file.directive';

describe('DndFileDirective', () => {
  it('should create an instance', () => {
    const directive = new DndFileDirective();
    expect(directive).toBeTruthy();
  });
});
